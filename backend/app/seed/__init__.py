"""Seed data reproducing the assignment wireframes.

Run with:

    docker compose exec api python -m app.seed          # insert if empty
    docker compose exec api python -m app.seed --reset  # wipe and reinsert
"""
