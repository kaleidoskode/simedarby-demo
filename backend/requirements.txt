# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.31.0
gunicorn==23.0.0
websockets==13.1
python-multipart==0.0.12

# --- Datastores ---
# MongoDB: catalogue, bookings and the seat_reservations unique index that
# makes double booking impossible.
motor==3.6.0
# Redis: seat locks (SET NX PX) and the per showtime event stream.
redis==5.2.1

# --- Configuration and auth ---
pydantic-settings==2.5.2
python-dotenv==1.0.1
pyjwt==2.9.0

# Screening times are resolved in the cinema's timezone via zoneinfo. Linux
# ships the IANA database, Windows does not, so this supplies it and keeps a
# local (non-Docker) run working on any host.
tzdata==2024.2

# --- HTTP client ---
httpx==0.27.2
