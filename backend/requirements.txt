# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.31.0
gunicorn==23.0.0
websockets==13.1
python-multipart==0.0.12

# --- Datastores ---
# MongoDB: catalog, bookings and the seat_reservations unique index that makes
# double booking impossible.
motor==3.6.0
# Redis: seat locks (SET NX PX) and the per showtime event stream.
redis==5.2.1
# MySQL layer retained from the base template; initialised lazily and unused by
# the cinema booking module.
sqlalchemy==2.0.35
pymysql==1.1.1

# --- Configuration and auth ---
pydantic-settings==2.5.2
python-dotenv==1.0.1
pyjwt==2.9.0

# --- HTTP client ---
httpx==0.27.2
